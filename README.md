# confederation

TypeScript configuration loader — merge, manage, and audit `.env` files, environment variables, and CLI flags with predictable precedence and Zod schema validation.

> **Status:** early / WIP — package surface still being designed.

## Packages

| Package | Description |
| --- | --- |
| [`@confederation/core`](_libs/core) | Core configuration loader (placeholder). |

## Development

```sh
pnpm install
pnpm libs:watch   # incremental TypeScript build in watch mode
```
